.logo {
  white-space: nowrap;
}
.logo .logo-img {
  position: relative;
  top: -3px;
  transition: all 0.3s ease;
}
.logo .logo-word {
  font-size: 18px;
  color: #fff;
  padding-left: 15px;
  line-height: 30px;
  height: 30px;
  transition: all 0.3s ease;
  overflow: hidden;
}
